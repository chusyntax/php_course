<?php
//PHP is a loosely typed language

//Variables are declared using the $ symbol - Must be together with the variable name
$name = "John";

//Used to dump out what the variable is holding
var_dump($name);


//Just declaring
$name2;

//Declaring and assigning
$name3 = "Jane";

//PHP data types - String, Integer, Float, Boolean, Array, Object, NULL, Resource
